{
  "name": "notes",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "$npm_execpath run build && wrangler dev",
    "deploy": "$npm_execpath run build && wrangler deploy",
    "notes:dates": "bun run scripts/update-note-dates.ts"
  },
  "dependencies": {
    "hono": "^4.13.2"
  },
  "devDependencies": {
    "@hono/vite-dev-server": "^0.26.1",
    "@hono/vite-ssg": "^0.3.3",
    "@types/bun": "^1.3.14",
    "gray-matter": "^4.0.3",
    "marked": "^18.0.9",
    "shiki": "^4.4.3",
    "vite": "^7.0.0",
    "wrangler": "^4.123.0"
  }
}
